/*var  byColor = [] ;
		
		$(".color-filter  input[type=checkbox]").on( "change", function() {
			if (this.checked) byColor.push(  $(this).attr("value")  );
			else removeA(byColor,   $(this).attr("value") );
			
			console.log(byColor);
			if (byColor.length >= 1){
			for ( var i = 0; i < byColor.length; i = i + 1 ) {
 
    console.log( '.products > .'+byColor[ i ] );
	$( '.products > .cat-box-warp' ).hide();
	$( '.products > .'+byColor[ i ] ).show();
 
}
			}else{
				console.log('no');
				$( '.products > .cat-box-warp' ).show();
			}
			
			
		});
		
			
		
			


		
		function removeA(arr) {
			var what, a = arguments, L = a.length, ax;
			while (L > 1 && arr.length) {
				what = a[--L];
				while ((ax= arr.indexOf(what)) !== -1) {
					arr.splice(ax, 1);
				}
			}
			return arr;
		}*/